[user]
	name = Ehryk
	email = rhaistlin@gmail.com
[http]
	sslVerify = false
[alias]
	a = add
	amend = commit --amend
	b = branch
	br = branch
	c = commit
	ci = commit
	cm = commit -m
	co = checkout
	d = diff
	dc = diff --cached
	dw = diff --word-diff
	hist = log --pretty=format:\" %ad | %s%d [%an]\" --graph --date=short
	linecount = "!lc() { git ls-files -z | xargs -0 wc -l; }; lc"
	lines = "!lc() { git ls-files -z ${1} | xargs -0 wc -l; }; lc"
	pu = pull
	pul = pull
	ph = push
	ps = push
	pus = push
	s = status
	st = status
[color]
	branch = auto
	diff = auto
	status = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
[merge]
	conflictstyle = diff3
[push]
	default = simple
